#include<bits/stdc++.h>
using namespace std;
#define double long double
const double eps = 1e-11;

double n,p1,v2,p2,v1;
double ans = 0.0;
bool check(double mid){
	double l1 = 
}
signed main(){
	int t;
	cin >> t;
	while(t--){	
		cin >> n >> p1 >> v1 >> p2 >> v2;
		if(p1 > p2){
			swap(p1,p2);
			swap(v1,v2);
		}
		double l = p1, r = p2;
		while(l < r){

		}
		printf("%.10Lf\n",l);
	}
}