#include<bits/stdc++.h>
using namespace std;
int dp[N],pre,a[N],n,k;
int main(){
	scanf("%d%d",&n,&k);
	for(int i=1;i<=n;i++)cin>>a[i];
	sort(a+1,a+n+1);
	for(int i=1;i<k;i++)dp[i]=2e9;
	for(int i=k;i<=n;i++){
		dp[i]=min(dp[])
	}
}
